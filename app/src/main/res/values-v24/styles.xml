<resources>

    <!--
      Use the opaque hand-off background instead of transparency when switching apps.
      android:windowBackgroundFallback is supposed to do this, but it wasn't working for
      me as of Android 7.1
    -->
    <style name="StreamBaseTheme" parent="AppBaseTheme">
        <item name="android:windowBackground">@drawable/stream_handoff_background</item>
    </style>

</resources>
